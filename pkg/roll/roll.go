package roll

import (
	"github.com/bwmarrin/discordgo"
)

// Roll rolls a dice, does the math requested, and returns the result.
func Roll(body string, message *discordgo.MessageCreate) (string, error) {
	return "", nil
}
